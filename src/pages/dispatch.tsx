import { Typography } from 'antd';

const { Title } = Typography;

export default function DispatchPage() {
  return (
    <div style={{ padding: 24 }}>
      <Title level={3}>Dispatch</Title>
      <p>This is the dispatch page.</p>
    </div>
  );
}