import { Typography } from 'antd';

const { Title } = Typography;

export default function BinningPage() {
  return (
    <div style={{ padding: 24 }}>
      <Title level={3}>Binning</Title>
      <p>This is the binning page.</p>
    </div>
  );
}